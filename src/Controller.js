import { useFrame } from '@react-three/fiber';
import gsap from 'gsap';
import { useEffect } from 'react';
const Controler = ({ sunRef, mercuryRef, currentFocus }) => {
  // useFrame((state) => {
  //   mercuryRef.current.rotation.y += (Math.random() + 0.2) * 0.002;

  //   console.log(mercuryRef.current.rotation);
  //   // console.log(mercuryRef.current.children[0].rotation.y);
  //   // state.camera.lookAt()

  //   // state.camera.lookAt(currentFocus.current.position);
  // });
  return <></>;
};

export default Controler;
